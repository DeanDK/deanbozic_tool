require "spec_helper"

describe DeanbozicTool do
  it "has a version number" do
    expect(DeanbozicTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
